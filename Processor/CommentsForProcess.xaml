<Window x:Class="Processor.CommentsForProcess"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Processor"
        mc:Ignorable="d"
        Title="CommentsToProcess" Height="450" Width="800">
    <Grid>
        <ListBox x:Name="CommentListBox" HorizontalAlignment="Left" Height="114" Margin="98,63,0,0" VerticalAlignment="Top" Width="534">
            <ListBox.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
            </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox x:Name="NewComment" MaxLength="150" HorizontalAlignment="Left" Height="92" Margin="98,205,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="534"/>
        <Button Click="AddNew_Click" Margin="98,341,581,53" RenderTransformOrigin="0.502,0.5">Add new</Button>
        <Button Click="Clear_Comments" Margin="519,341,160,53" RenderTransformOrigin="0.502,0.5" Content="Clear Comments"/>
    </Grid>
</Window>
